package types

import (
	pkgtypes "github.com/ShubhamDX/aion/pkg/types"
)

// Type aliases — all existing internal imports compile unchanged.
type ChatCompletionRequest = pkgtypes.ChatCompletionRequest
type Message = pkgtypes.Message
type ToolCall = pkgtypes.ToolCall
type FunctionCall = pkgtypes.FunctionCall
type Tool = pkgtypes.Tool
type FunctionDef = pkgtypes.FunctionDef
type ResponseFormat = pkgtypes.ResponseFormat
