package types

import (
	pkgtypes "github.com/ShubhamDX/aion/pkg/types"
)

// Type aliases — all existing internal imports compile unchanged.
type Tier = pkgtypes.Tier

const (
	Tier1 = pkgtypes.Tier1
	Tier2 = pkgtypes.Tier2
	Tier3 = pkgtypes.Tier3
)

type AIONPreferences = pkgtypes.AIONPreferences
type RoutingDecision = pkgtypes.RoutingDecision
type RequestEvent = pkgtypes.RequestEvent
